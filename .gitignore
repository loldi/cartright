# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/

# Env / secrets
.env
.env.*
!.env.example
*.db
*.sqlite
*.sqlite3
# Order-history JSON is real purchase data (PII) — never commit it.
orders.json
*orders*.json

# Local-only seed/import tooling: operates on real personal data, excluded per PRD.
scripts/local/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Logs
*.log

# Agent-internal workflow files (not part of the public product)
CLAUDE.md
RETROSPECTIVE.md
.claude/
review-sample.html

# Working drafts (local-only until promoted into a tracked doc)
*.draft.md

# Design artifacts, not product code
Cartright Architecture Deck.pdf
Cartright Architecture Deck-v2.pdf
DESIGN.md

# Vendor API docs (reference only, not redistributable)
io-docs/
